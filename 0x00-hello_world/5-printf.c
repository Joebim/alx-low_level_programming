#include <stdio.h>

/**
 * main - head function
 * printf - function printing strring
 * return: 0 if code runs
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
