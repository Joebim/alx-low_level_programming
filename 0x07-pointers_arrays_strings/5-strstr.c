#include "main.b"
#include <string.h>

/**
 * *_strstr - function finds the first occurrence of the substring
 * @haystack: string
 * @needle: string
 * Return: output of function
 */

char *_strstr(char *haystack, char *needle)
{
	return (sttstr(haystack, needle));
}
