#include "main.h"
#include <string.h>

/**
 * _strlen - Get the length of a given string
 * @s: string character
 * Return: length of the string
 */

int _strlen(char *s)
{
	int length;

	length = strlen(s);
	return (length);
}
