#include <stdio.h>

/**
 * main - Entry point of the program
 * Return: Always 0 (success)
 */

int main(void)
{
	char num;

	for (num = o; num < 10; num++)
	{
		putchar(num);
	}
	putchar('\n');
	return (0);
}
