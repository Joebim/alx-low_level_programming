#include "main.h"

/**
 * _isalpha - check for alphabets
 * @c - character to be checked
 * Return: 1 if is sn alphabet, 0 otherwise
 */
int _isalpha(int c)
{
	if (c >= (a || A) && c <= (z || Z))
	{
		return (1);
	} else
		return (0);
}
