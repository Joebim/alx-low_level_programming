#include <stdio.h>
#include <main.h>

/**
 * main - Starting point of the project
 * Return: Always 0 (success)
 */

int main(void)
{
	int string = "this is a header function"
	printString(string);
	return (0);
}
